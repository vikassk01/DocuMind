package com.documind.demo.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class VectorStoreConfig {
    // Intentionally empty - vector store now managed in DocumentService
}